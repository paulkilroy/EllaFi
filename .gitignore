.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
data/config.json*
.DS_Store
.claude/
__pycache__/
*.pyc
# mock runtime state (persisted node aliases)
test/mock_clients.json

# Security audit — keep out of the (public) repo
memory/code_audit.md

# HAR captures — contain raw credentials/cookies; process via test/har_to_catalog.py instead
*.har
web/map.svg
data/map.svg

# Local voucher-group backups (contain live voucher codes) — do not commit
voucher_backups/

# sim/co-sim generated outputs
kicad/sim/board/*.dat
kicad/sim/board/cosim_traces.json
kicad/sim/board/scope.html
kicad/fab/
omada-html/ellafi-poc.zip
tools/uptime_report.html
# Proprietary decompiled/extracted TP-Link controller code — analysis only, never commit
omada-lab/_jars/
omada-lab/_jadx*/
omada-lab/_cls/
omada-lab/_focus/
omada-lab/_recover/
omada-lab/omada_recon_report.md
# lab-local + runtime artifacts
omada-lab/.env
omada-lab/emulator/*.local
omada-lab/emulator/*.log
omada-lab/emulator/_inject*.jsonl
omada-lab/emulator/__pycache__/
omada-lab/*.log
# secrets / keys
omada-lab/findings-secrets.local
omada-lab/_key.pem
omada-lab/rc4key.local
omada-lab/captured-config.local

# editor/local + large reference artifacts (keep on disk, out of git)
*.kicad_prl
/demo.zip
/ad61d594-*.png
